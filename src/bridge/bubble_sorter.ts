import { Sorter } from "./sorter";
import { SortImple } from "./sort_imple";

export class BubbleSorter extends SortImple {
    public sort(objs: Object[]): void {
        // バブルソートで obj[] をソートする
    }
}